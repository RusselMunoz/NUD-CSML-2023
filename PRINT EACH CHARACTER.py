# print each character
text = input("Enter a string: ")
for x in text:
    print(x)

# print each character (using range)
text = input("Enter a string: ")
for x in range(len(text)):
    print(text[x])